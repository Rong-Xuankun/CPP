#include <stdio.h>
int main()
{
    int T;
    int i;
    char path[10][100];
    for(T = 0; T < 10; T++)
    {
        gets(path[T]);
        for(i = 0; path[i] != '\0'; i++)
        {
            
        }
    }
}